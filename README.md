# Yutori Ledger

スマホで支出、固定費、ローン返済をまとめて見るためのPWAです。

## 方針

- 支出は手入力で素早く記録する
- メルカリやカードのログイン情報は保存しない
- 入力データはブラウザのローカルストレージに保存する
- 返済と予備費を見ながら、今月あと使える金額を表示する
- 個人情報を公開リポジトリに置かず、JSONで端末ごとに読み込む

## 開発

```bash
npm install
npm run dev
```

## ビルド

```bash
npm run build
```
